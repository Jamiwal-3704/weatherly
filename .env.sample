# Example .env file (for reference only)
# This file is not used directly by the browser. For local static hosting,
# create `config.js` (see config.sample.js) and set the key there.

OPENWEATHER_API_KEY=YOUR_OPENWEATHER_API_KEY_HERE
