/*
  Copy this file to `config.js` and replace the placeholder with your real API key.
  `config.js` is gitignored so your key won't be committed.

  Example:
  const CONFIG = { OPENWEATHER_API_KEY: 'your_real_key_here' };
*/

const CONFIG = {
  OPENWEATHER_API_KEY: "YOUR_OPENWEATHER_API_KEY_HERE",
};
